<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Results - Notes App</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <main class="w-full min-h-screen bg-zinc-100 font-['Helvetica']">
        <nav class="flex px-10 justify-between items-center py-5 bg-white shadow-md rounded-b-xl">
            <h3 class="text-3xl font-semibold tracking-tight text-blue-600">Notes App</h3>
            <div class="navlinks flex gap-5 text-lg font-medium text-gray-700">
                <a class="hover:text-blue-600 transition" href="/">Home</a>
                <a class="hover:text-blue-600 transition" href="/create">New Note</a>
                <a class="hover:text-blue-600 transition" href="/dashboard">Analytics Dashboard</a>
            </div>
        </nav>

        <div class="px-10 py-6">
            <h3 class="capitalize text-3xl font-semibold mb-5 mt-10 tracking-tight text-gray-800">Search Results</h3>

            <% if (notes && notes.length > 0) { %>
                <p class="mb-4 text-lg text-gray-600">Found <%= notes.length %> result(s) for "<%= query %>"</p>

                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                    <% notes.forEach(note => { %>
                    <div class="note max-w-sm p-5 bg-white shadow-lg rounded-lg transition transform hover:shadow-2xl">
                        <div class="flex justify-between items-center mb-3">
                            <h3 class="text-xl font-semibold text-gray-800 w-3/4 truncate"><%= note.date %></h3>
                            <a href="/note/<%= note.id %>"
                                class="w-8 h-8 bg-blue-500 text-white flex items-center justify-center rounded-full shadow">
                                <i class="ri-arrow-right-line"></i>
                            </a>
                        </div>
                        <p class="text-gray-600 mb-4 truncate">
                            <%= note.content.split('\n')[0] %>...
                        </p>
                        <div class="flex justify-between items-center">
                            <a href="/edit/<%= note.id %>" class="text-blue-500 hover:text-blue-600 transition duration-200">
                                <i class="ri-pencil-line"></i> Edit
                            </a>
                            <form action="/delete/<%= note.id %>" method="get">
                                <button type="submit" class="text-red-500 hover:text-red-600 transition duration-200">
                                    <i class="ri-delete-bin-line"></i> Delete
                                </button>
                            </form>
                        </div>
                    </div>
                    <% }); %>
                </div>
            <% } else { %>
                <p class="text-zinc-500 text-lg">No results found for "<%= query %>"</p>
            <% } %>

            <a href="/" class="mt-6 inline-block bg-blue-500 text-white font-semibold py-2 px-6 rounded shadow hover:bg-blue-600 transition duration-200">Back to Home</a>
        </div>
    </main>
</body>

</html>
