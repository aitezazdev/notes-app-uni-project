<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Note</title>
    <link rel="stylesheet" href="/public/css/style.css">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <main class="w-full h-screen bg-zinc-100 font-['Helvetica']">
        <nav class="flex px-10 justify-between py-5">
            <h3 class="text-2xl tracking-tight">Notes App</h3>
            <div class="navlinks flex gap-5">
                <a class="tracking-tight" href="/">Home</a>
                <a class="tracking-tight" href="/create">Create New Note</a>
            </div>
        </nav>
        <div class="px-10 notes">
            <h3 class="capitalize text-2xl font-medium mb-5 mt-10 tracking-tight">Edit Note</h3>
            <form action="/update/<%= note.id %>" method="post">
                <input name="date" class="text-md px-3 py-2 w-1/2 mb-5 rounded-md bg-zinc-200" type="text" value="<%= note.date %>" required>
                <textarea name="content" class="text-md px-3 py-2 w-full h-32 mb-5 rounded-md bg-zinc-200" required><%= note.content %></textarea>
                <button type="submit" class="px-4 py-2 bg-blue-600 text-white rounded-md">Update Note</button>
            </form>
        </div>
    </main>
</body>

</html>
