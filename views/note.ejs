<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Note Details</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <main class="w-full h-screen bg-zinc-100 font-['Helvetica']">
        <nav class="flex px-10 justify-between py-5">
            <h3 class="text-2xl tracking-tight">Notes App</h3>
            <div class="navlinks flex gap-5">
                <a class="tracking-tight" href="/">Home</a>
                <a class="tracking-tight" href="/create">Create New Note</a>
                <a class="tracking-tight" href="/dashboard">Analytics Dashboard</a>
            </div>
        </nav>

        <div class="px-10 notes">
            <h3 class="text-2xl font-medium mb-5 mt-10 tracking-tight"><%= note.name %></h3>
            <h4 class="text-xl font-semibold">Note Name: <%= note.date %></h4>
            <p class="font-medium text-xl mb-10 mt-5"><%= note.content %></p>
            <a class="text-zinc-400" href="/edit/<%= note.id %>">Edit this Note</a>
        </div>
    </main>
</body>

</html>