<!DOCTYPE html>
<html lang="en" class="h-full bg-gray-50">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notes Analytics Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <style>
        .note-name {
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
            max-width: 100%;
        }
    </style>
</head>
<body class="h-full">
    <div class="min-h-full flex flex-col">
        <nav class="flex px-10 justify-between items-center py-5 bg-white shadow-md rounded-b-xl">
            <h3 class="text-3xl font-semibold tracking-tight text-blue-600">Notes App</h3>
            <div class="navlinks flex gap-5 text-lg font-medium text-gray-700">
                <a class="hover:text-blue-600 transition" href="/">Home</a>
                <a class="hover:text-blue-600 transition " href="/create">New Note</a>
                <a class="hover:text-blue-600 transition border-b-2 border-blue-500" href="/dashboard">Analytics Dashboard</a>
            </div>
        </nav>

        <main class="flex-grow flex items-center justify-center px-4 sm:px-6 lg:px-8 py-12">
            <div class="max-w-5xl w-full">
                <div class="bg-white shadow-xl rounded-lg overflow-hidden">
                    <div class="px-6 py-8 border-b border-gray-200">
                        <h2 class="text-3xl font-extrabold text-gray-900 leading-tight">Notes Analytics</h2>
                        <p class="mt-2 text-lg text-gray-600">A comprehensive overview of your notes statistics</p>
                    </div>
                    <div class="px-6 py-8">
                        <dl class="grid grid-cols-1 gap-x-6 gap-y-10 sm:grid-cols-3">
                            <div class="sm:col-span-1">
                                <dt class="text-lg font-medium text-gray-600">Total Notes</dt>
                                <dd class="mt-2 text-5xl font-extrabold text-blue-600"><%= stats.totalNotes %></dd>
                            </div>
                            <div class="sm:col-span-1">
                                <dt class="text-lg font-medium text-gray-600">Shortest Note</dt>
                                <dd class="mt-2 text-base text-gray-900">
                                    <% if (stats.shortestId !== -1 && stats.noteDetails[stats.shortestId]) { %>
                                        <p class="text-xl font-semibold"><%= stats.shortestNote %> characters</p>
                                        <p class="text-sm text-gray-500 note-name mt-1">Name: <%= stats.noteDetails[stats.shortestId].date %></p>
                                        <a href="/note/<%= stats.noteDetails[stats.shortestId].id %>" class="mt-3 inline-flex items-center px-4 py-2 border border-transparent text-base font-medium rounded-md shadow-sm text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 transition duration-150 ease-in-out">
                                            View Note <i class="ri-arrow-right-line ml-2"></i>
                                        </a>
                                    <% } else { %>
                                        <span class="text-xl text-gray-500">No data available</span>
                                    <% } %>
                                </dd>
                            </div>
                            <div class="sm:col-span-1">
                                <dt class="text-lg font-medium text-gray-600">Longest Note</dt>
                                <dd class="mt-2 text-base text-gray-900">
                                    <% if (stats.longestId !== -1 && stats.noteDetails[stats.longestId]) { %>
                                        <p class="text-xl font-semibold"><%= stats.longestNote %> characters</p>
                                        <p class="text-sm text-gray-500 note-name mt-1">Name: <%= stats.noteDetails[stats.longestId].date %></p>
                                        <a href="/note/<%= stats.noteDetails[stats.longestId].id %>" class="mt-3 inline-flex items-center px-4 py-2 border border-transparent text-base font-medium rounded-md shadow-sm text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 transition duration-150 ease-in-out">
                                            View Note <i class="ri-arrow-right-line ml-2"></i>
                                        </a>
                                    <% } else { %>
                                        <span class="text-xl text-gray-500">No data available</span>
                                    <% } %>
                                </dd>
                            </div>
                        </dl>
                    </div>
                </div>
            </div>
        </main>

        <footer class="bg-white">
            <div class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
                <p class="text-center text-base text-gray-500">&copy; 2024 Notes App. All rights reserved.</p>
            </div>
        </footer>
    </div>
</body>
</html>