<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notes Analytics Dashboard</title>
    <link rel="stylesheet" href="/css/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        .note-name {
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
            max-width: 100%;
        }
    </style>
</head>
<body class="bg-gray-100 font-sans">
    <main class="min-h-screen">
        <nav class="flex px-10 justify-between items-center py-5 bg-white shadow-md rounded-b-xl">
            <h3 class="text-3xl font-semibold tracking-tight text-blue-600">Notes App</h3>
            <div class="navlinks flex gap-5 text-lg font-medium text-gray-700">
                <a class="hover:text-blue-600 transition" href="/">Home</a>
                <a class="hover:text-blue-600 transition" href="/create">New Note</a>
                <a class="hover:text-blue-600 transition" href="/dashboard">Analytics Dashboard</a>
            </div>
        </nav>

        <div class="container mx-auto p-6">
            <h2 class="text-2xl font-semibold mb-6 text-gray-800">Notes Analytics</h2>

            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                <!-- Total Notes Card -->
                <div class="bg-white p-5 rounded-lg shadow-lg transition-transform transform">
                    <h4 class="text-xl font-medium text-gray-700">Total Notes:</h4>
                    <p class="text-7xl pt-4 text-blue-600 text-center font-bold"><%= stats.totalNotes %></p>
                </div>

                <!-- Shortest Note Card -->
                <div class="bg-white p-5 rounded-lg shadow-lg transition-transform transform">
                    <h4 class="text-xl font-medium text-gray-700">Shortest Note:</h4>
                    <p class="text-lg text-center py-5 text-blue-600">
                        <% if (stats.shortestId !== -1 && stats.noteDetails[stats.shortestId]) { %>
                            <span>Length: <%= stats.shortestNote %> characters</span>
                            <br>
                            <span class="text-sm text-gray-500 note-name">Note Name: <%= stats.noteDetails[stats.shortestId].date %></span>
                            <a href="/note/<%= stats.noteDetails[stats.shortestId].id %>" class="mt-2 inline-block bg-blue-500 text-white font-semibold py-2 px-4 rounded shadow hover:bg-blue-600 transition duration-200 hover:shadow-lg">View</a>
                        <% } else { %>
                            <span class="text-gray-500">No shortest Note available.</span>
                        <% } %>
                    </p>
                </div>

                <!-- Longest Note Card -->
                <div class="bg-white p-6 rounded-lg shadow-lg transition-transform transform">
                    <h4 class="text-lg font-medium text-gray-700">Longest Note:</h4>
                    <p class="text-xl text-blue-600 text-center">
                        <% if (stats.longestId !== -1 && stats.noteDetails[stats.longestId]) { %>
                            <span>Length: <%= stats.longestNote %> characters</span>
                            <br>
                            <span class="text-sm text-gray-500 note-name">Note Name: <%= stats.noteDetails[stats.longestId].date %></span>
                            <a href="/note/<%= stats.noteDetails[stats.longestId].id %>" class="mt-2 inline-block bg-blue-500 text-white font-semibold py-2 px-4 rounded shadow hover:bg-blue-600 transition duration-200 hover:shadow-lg">View</a>
                        <% } else { %>
                            <span class="text-gray-500">No longest Note available.</span>
                        <% } %>
                    </p>
                </div>
            </div>
        </div>
    </main>

    <footer class="bg-white">
        <div class="max-w-5xl mx-auto py-4 px-4 sm:px-6 lg:px-8">
            <p class="text-center text-sm text-gray-500">&copy; 2024 Notes App. All rights reserved.</p>
        </div>
    </footer>
</body>
</html>