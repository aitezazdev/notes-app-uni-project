<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notes App - Home</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Add custom styles for the body gradient background */
        body {
            background: linear-gradient(135deg, #f5f7fa, #c3cfe2);
        }
    </style>
</head>
<body>
    <main class="w-full min-h-screen bg-transparent">
        <!-- Navbar -->
        <nav class="flex px-10 justify-between items-center py-5 bg-white shadow-md rounded-b-xl">
            <h3 class="text-3xl font-semibold tracking-tight text-blue-600">Notes App</h3>
            <div class="navlinks flex items-center gap-5 text-lg font-medium text-gray-700">
                <a class="hover:text-blue-600 transition" href="/">Home</a>
                <a class="hover:text-blue-600 transition" href="/create">New Note</a>
                <a class="hover:text-blue-600 transition" href="/dashboard">Analytics Dashboard</a>
                <form action="/search" method="GET" class="flex items-center">
                    <input type="text" name="query" placeholder="Search notes by name" required
                        class="flex-grow px-4 py-2 rounded-l-md border-2 border-blue-500 focus:outline-none focus:border-blue-600">
                    <button type="submit"
                        class="bg-blue-500 text-white px-4 py-2 rounded-r-md border-2 border-blue-500 hover:bg-blue-600 hover:border-blue-600 transition duration-200">
                        Search
                    </button>
                </form>
            </div>
        </nav>

        <!-- Notes Section -->
        <div class="px-10 mt-10">
            <h3 class="capitalize text-3xl font-bold mb-8 text-gray-800">All Notes</h3>
            <% if (notes.length === 0) { %>
                <div class="text-center py-10">
                    <p class="text-xl text-gray-600 mb-4">No notes to display</p>
                    <a href="/create" class="bg-blue-500 text-white px-6 py-3 rounded-md hover:bg-blue-600 transition duration-200">
                        Add a New Note
                    </a>
                </div>
            <% } else { %>
                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                    <% notes.forEach(note => { %>
                    <div class="note max-w-sm p-5 bg-white shadow-lg rounded-lg transition transform hover:shadow-2xl">
                        <div class="flex justify-between items-center mb-3">
                            <h3 class="text-xl font-semibold text-gray-800 w-3/4 truncate"><%= note.date %></h3>
                            <a href="/note/<%= note.id %>"
                                class="w-8 h-8 bg-blue-500 text-white flex items-center justify-center rounded-full shadow">
                                <i class="ri-arrow-right-line"></i>
                            </a>
                        </div>
                        <p class="text-gray-600 mb-4 truncate">
                            <%= note.content.split('\n')[0] %>...
                        </p>
                        <div class="flex justify-between items-center">
                            <a href="/edit/<%= note.id %>" class="text-blue-500 hover:text-blue-600 transition duration-200">
                                <i class="ri-pencil-line"></i> Edit
                            </a>
                            <form action="/delete/<%= note.id %>" method="get">
                                <button type="submit" class="text-red-500 hover:text-red-600 transition duration-200">
                                    <i class="ri-delete-bin-line"></i> Delete
                                </button>
                            </form>
                        </div>
                    </div>
                    <% }); %>
                </div>
            <% } %>
        </div>
    </main>

    <footer class="bg-white">
        <div class="max-w-5xl mx-auto py-4 px-4 sm:px-6 lg:px-8">
            <p class="text-center text-sm text-gray-500">&copy; 2024 Notes App. All rights reserved.</p>
        </div>
    </footer>
</body>
</html>